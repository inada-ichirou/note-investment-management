services:
  - type: cron
    name: note-investment-scheduler
    env: node
    buildCommand: npm install
    startCommand: npm start
    schedule: "0 */3 * * *"  # 3時間ごと（0, 3, 6, 9, 12, 15, 18, 21時）
    envVars:
      - key: NODE_ENV
        value: production
      - key: CI
        value: true 
